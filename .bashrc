#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

source ~/.config/bash/utils.bash
source ~/.config/bash/env.bash

source ~/.config/bash/aliases.bash
source ~/.config/bash/prompt.bash
source ~/.config/bash/run.bash
source ~/.config/bash/binds.bash
